Clone the repo and create the following symlinks:

```shell
ln -s conf/cygwin/.bashrc ~/.bashrc
ln -s conf/cygwin/.config ~/.config
ln -s conf/cygwin/.minttyrc ~/.minttyrc
ln -s conf/cygwin/.vimrc ~/.vimrc
```
